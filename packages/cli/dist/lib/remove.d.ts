/**
 * Remove or Delete subgraph from custom graph node
 */
export function removeSubgraph({ subgraphName, node, accessToken }: {
    subgraphName: any;
    node: any;
    accessToken: any;
}): Promise<void>;
